name: DEPRECATED — replaced by ci.yml and cd.yml

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. Use ci.yml (manual CI) and cd.yml (auto CD) instead."
